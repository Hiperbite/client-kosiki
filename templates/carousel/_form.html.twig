{{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> Save
    </button>
    <a href="{{ path('app_carousel_index') }}" class="btn">
        <i class="fa fa-list-alt" aria-hidden="true"></i> Back to list
    </a>
{{ form_end(form) }}
